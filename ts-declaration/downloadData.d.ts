/**
 * Download data as a file.
 * @param {object} data
 * @param {string} fileName
 * @param {string} fileType
 */
export function downloadData(data: object, fileName: string, fileType: string): void;
